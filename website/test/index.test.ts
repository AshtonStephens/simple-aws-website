import * as index from "../src/index";

describe("TestButtonCalls", () => {

  beforeEach(() => {
    // To do before each test, set up mock api.
  });

  it("should get the message count", async () => {
    // TODO: Write tests
  });

  it("should create a message", async () => {
    // TODO: Write tests
  });

  it("should get a message", async () => {
    // TODO: Write tests
  });
});
